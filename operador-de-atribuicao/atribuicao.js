/*
    Operadores de atribuição

    podem funcionar como formas abreviadas
*/

    let a = 5;
    let b = 10;
    a += b;//  a = a + b (15)
    a -= b;// a = a - b(-5)
    a *= b;// a = a * b (50)
    a /= b;// a = a / b (0.5)
    a %= b;// a = a % b (0)
    a **= b;// a = a ** b (9765625)
    
