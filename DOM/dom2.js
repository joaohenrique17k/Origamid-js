    const pegarH = document.querySelector('.h1zin');
    console.log(pegarH)

    pegarH.innerHTML;//Retorna o texto;
    pegarH.classList; // Retorna as classes;
    pegarH.id;//retorn o id;
    pegarH.offsetHeight//Retorna a altura do elemento

    pegarH.addEventListener('click',callback);
    //Ativa a função de callback ao clicar no titulo
