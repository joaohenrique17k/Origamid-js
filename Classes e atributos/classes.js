/*
        ClassList

        Retorna um lista com as classes do elemnto. Permite adicionar,remover e verificar 
        se contém.



    const menu = document.querySelector('.menu');

    menu.className; //String
    menu.classList; //Lista de classes
    menu.classList.add('ativo');//Adiciona uma nova classe para o menu
    menu.classList.add('ativo', 'mobile');// Adicione duas classes
    menu.classList.remove('ativo');//remove a classe
    menu.classList.toggle('ativo');//adiciona/remove a classe
    menu.classList.contains('ativo');//true ou false(verifica se existe ou não)
    menu.classList.replace('ativo','inativo');//subistitui uma classe por outra 

*/